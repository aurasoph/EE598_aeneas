-- THIS FILE WAS AUTOMATICALLY GENERATED BY AENEAS
-- [binary_heap]
import Aeneas
open Aeneas Aeneas.Std Result ControlFlow Error
set_option linter.dupNamespace false
set_option linter.hashCommand false
set_option linter.unusedVariables false

/- You can set the `maxHeartbeats` value with the `-max-heartbeats` CLI option -/
set_option maxHeartbeats 1000000

/- You can remove the following line by using the CLI option `-all-computable`: -/
noncomputable section

namespace binary_heap

/- [alloc::vec::{alloc::vec::Vec<T>}::pop]:
   Source: '/rustc/library/alloc/src/vec/mod.rs', lines 2725:4-2725:38
   Name pattern: [alloc::vec::{alloc::vec::Vec<@T>}::pop] -/
@[rust_fun "alloc::vec::{alloc::vec::Vec<@T>}::pop"]
axiom alloc.vec.Vec.pop
  {T : Type} (A : Type) :
  alloc.vec.Vec T → Result ((Option T) × (alloc.vec.Vec T))

/- [alloc::vec::{alloc::vec::Vec<T>}::is_empty]:
   Source: '/rustc/library/alloc/src/vec/mod.rs', lines 2956:4-2956:40
   Name pattern: [alloc::vec::{alloc::vec::Vec<@T>}::is_empty] -/
@[rust_fun "alloc::vec::{alloc::vec::Vec<@T>}::is_empty"]
axiom alloc.vec.Vec.is_empty
  {T : Type} (A : Type) : alloc.vec.Vec T → Result Bool

/- [binary_heap::MinHeap]
   Source: 'src/lib.rs', lines 4:0-6:1 -/
structure MinHeap where
  a : alloc.vec.Vec Std.I32

/- [binary_heap::{binary_heap::MinHeap}::new]:
   Source: 'src/lib.rs', lines 9:4-11:5 -/
def MinHeap.new : Result MinHeap := do
  ok { a := (alloc.vec.Vec.new Std.I32) }

/- [binary_heap::{binary_heap::MinHeap}::is_empty]:
   Source: 'src/lib.rs', lines 13:4-15:5 -/
def MinHeap.is_empty (self : MinHeap) : Result Bool := do
  alloc.vec.Vec.is_empty Global self.a

/- [binary_heap::{binary_heap::MinHeap}::push]: loop 0:
   Source: 'src/lib.rs', lines 1:0-27:9 -/
def MinHeap.push_loop
  (v : alloc.vec.Vec Std.I32) (i : Std.Usize) :
  Result (alloc.vec.Vec Std.I32)
  := do
  loop
    (fun (v1, i1) =>
      if i1 > 0#usize
      then
        do
        let i2 ← i1 - 1#usize
        let p ← i2 / 2#usize
        let i3 ←
          alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice Std.I32)
            v1 p
        let i4 ←
          alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice Std.I32)
            v1 i1
        if i3 <= i4
        then ok (done v1)
        else
          let (s, deref_mut_back) ← lift (alloc.vec.Vec.deref_mut v1)
          let s1 ← core.slice.Slice.swap s p i1
          let v2 := deref_mut_back s1
          ok (cont (v2, p))
      else ok (done v1))
    (v, i)

/- [binary_heap::{binary_heap::MinHeap}::push]:
   Source: 'src/lib.rs', lines 17:4-28:5 -/
def MinHeap.push (self : MinHeap) (x : Std.I32) : Result MinHeap := do
  let v ← alloc.vec.Vec.push self.a x
  let i := alloc.vec.Vec.len v
  let i1 ← i - 1#usize
  let v1 ← MinHeap.push_loop v i1
  ok { a := v1 }

/- [binary_heap::{binary_heap::MinHeap}::sift_down]: loop 0:
   Source: 'src/lib.rs', lines 1:0-60:9 -/
def MinHeap.sift_down_loop
  (v : alloc.vec.Vec Std.I32) (i : Std.Usize) (n : Std.Usize) :
  Result (alloc.vec.Vec Std.I32)
  := do
  loop
    (fun (v1, i1) =>
      do
      let i2 ← 2#usize * i1
      let l ← i2 + 1#usize
      let r ← i2 + 2#usize
      if l >= n
      then ok (done v1)
      else
        let m ←
          if r < n
          then
            let i3 ←
              alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice
                Std.I32) v1 r
            let i4 ←
              alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice
                Std.I32) v1 l
            if i3 < i4
            then ok r
            else ok l
          else ok l
        let i3 ←
          alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice Std.I32)
            v1 i1
        let i4 ←
          alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice Std.I32)
            v1 m
        if i3 <= i4
        then ok (done v1)
        else
          let (s, deref_mut_back) ← lift (alloc.vec.Vec.deref_mut v1)
          let s1 ← core.slice.Slice.swap s i1 m
          let v2 := deref_mut_back s1
          ok (cont (v2, m)))
    (v, i)

/- [binary_heap::{binary_heap::MinHeap}::sift_down]:
   Source: 'src/lib.rs', lines 43:4-61:5 -/
def MinHeap.sift_down (self : MinHeap) (i : Std.Usize) : Result MinHeap := do
  let n := alloc.vec.Vec.len self.a
  let v ← MinHeap.sift_down_loop self.a i n
  ok { a := v }

/- [binary_heap::{binary_heap::MinHeap}::pop_min]:
   Source: 'src/lib.rs', lines 30:4-41:5 -/
def MinHeap.pop_min
  (self : MinHeap) : Result ((Option Std.I32) × MinHeap) := do
  let b ← alloc.vec.Vec.is_empty Global self.a
  if b
  then ok (none, self)
  else
    let min ←
      alloc.vec.Vec.index (core.slice.index.SliceIndexUsizeSlice Std.I32)
        self.a 0#usize
    let (o, v) ← alloc.vec.Vec.pop Global self.a
    let last ← core.option.Option.unwrap o
    let b1 ← alloc.vec.Vec.is_empty Global v
    if b1
    then ok (some min, { a := v })
    else
      let (_, index_mut_back) ←
        alloc.vec.Vec.index_mut (core.slice.index.SliceIndexUsizeSlice Std.I32)
          v 0#usize
      let v1 := index_mut_back last
      let self1 ← MinHeap.sift_down { a := v1 } 0#usize
      ok (some min, self1)

end binary_heap
